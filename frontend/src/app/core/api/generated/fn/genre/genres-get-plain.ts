/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GenreDto } from '../../models/genre-dto';

export interface GenresGet$Plain$Params {
}

export function genresGet$Plain(http: HttpClient, rootUrl: string, params?: GenresGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GenreDto>>> {
  const rb = new RequestBuilder(rootUrl, genresGet$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GenreDto>>;
    })
  );
}

genresGet$Plain.PATH = '/genres';
