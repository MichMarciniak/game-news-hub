/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameDetailsResponse } from '../../models/game-details-response';

export interface GameGameIdGet$Plain$Params {
  gameId: (number | string);
}

export function gameGameIdGet$Plain(http: HttpClient, rootUrl: string, params: GameGameIdGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<GameDetailsResponse>> {
  const rb = new RequestBuilder(rootUrl, gameGameIdGet$Plain.PATH, 'get');
  if (params) {
    rb.path('gameId', params.gameId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GameDetailsResponse>;
    })
  );
}

gameGameIdGet$Plain.PATH = '/Game/{gameId}';
