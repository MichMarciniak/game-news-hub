/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameListResponse } from '../../models/game-list-response';

export interface GameListGet$Json$Params {
}

export function gameListGet$Json(http: HttpClient, rootUrl: string, params?: GameListGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GameListResponse>>> {
  const rb = new RequestBuilder(rootUrl, gameListGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GameListResponse>>;
    })
  );
}

gameListGet$Json.PATH = '/Game/list';
