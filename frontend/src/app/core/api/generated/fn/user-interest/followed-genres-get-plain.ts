/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GenreDto } from '../../models/genre-dto';

export interface FollowedGenresGet$Plain$Params {
}

export function followedGenresGet$Plain(http: HttpClient, rootUrl: string, params?: FollowedGenresGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<GenreDto>> {
  const rb = new RequestBuilder(rootUrl, followedGenresGet$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GenreDto>;
    })
  );
}

followedGenresGet$Plain.PATH = '/followed/genres';
